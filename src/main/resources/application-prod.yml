server:
  port: 8222

eureka:
  instance:
    hostname: gateway-tyir.onrender.com
  client:
    serviceUrl:
      defaultZone: https://bankati-discovery-latest.onrender.com/
    fetch-registry: true
    register-with-eureka: true
  server:
    enable-self-preservation: false
