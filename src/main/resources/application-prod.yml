server:
  port: 8222

eureka:
  instance:
    hostname: bankatii-gateway-crypto.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery.onrender.com/eureka/}
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false

